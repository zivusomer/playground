$kind: collection
name: 'Simple Common APIs'
description: |-
  A small set of common REST-style requests for quickly testing typical CRUD patterns.

  **How to use**
  - Select the *Development* or *Production* environment.
  - Ensure `baseURL` is set correctly.
  - (Optional) Set `token` for the Protected endpoint.
