$kind: http-request
name: 'Delete user'
method: DELETE
url: '{{baseURL}}/users/:id'
order: 5000
description: |-
  Deletes a user by id.

  Uses a path variable example (`id`).
pathVariables:
  - key: id
    value: '1'
headers:
  - key: Accept
    value: application/json
