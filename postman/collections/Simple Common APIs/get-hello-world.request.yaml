$kind: http-request
name: 'Hello world'
method: GET
url: '{{baseURL}}/hello-world'
order: 8000
description: |-
  Node.js API: hello-world endpoint.

  Expected to return JSON like `{ "message": "Hello, World!" }`.
headers:
  - key: Accept
    value: application/json
