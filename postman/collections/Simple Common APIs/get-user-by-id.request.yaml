$kind: http-request
name: 'Get user by id'
method: GET
url: '{{baseURL}}/users/:id'
order: 2000
description: |-
  Fetch a single user by id.

  Uses a path variable example (`id`).
pathVariables:
  - key: id
    value: '1'
headers:
  - key: Accept
    value: application/json
